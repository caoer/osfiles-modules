-- No custom clipboard config needed.
-- LazyVim sets clipboard=unnamedplus (everything syncs to system clipboard).
-- yanky.nvim (enabled in lazyvim.json) provides a yank ring:
--   p then <C-p>/<C-n> to cycle through previous yanks/deletes.
return {}
